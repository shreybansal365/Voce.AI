apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: voce
spec:
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30002
  type: NodePort

